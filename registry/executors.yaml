# 执行节点登记（可选，个人版默认不启用）
# 仅当拥有多台个人 PC 且需要分发构建/测试时使用；
# 启用方式：取消注释并按实填写 → 提交 MR → control-api 热加载
# executors:
#   - executor_id: pc-01
#     tags: [java17, node20, pnpm]   # 能力标签（按本机工具链探测）
#     slots: 1                       # 并发槽位（个人版无昼夜时段策略）
#     token_ref: env:EXECUTOR_TOKEN_PC01   # 认证 token 引用，不存明文
executors: []
